# secrets — config.json carries the live API key (config.example.json is the template)
config.json
# ship secrets — the Discord release-announce webhook
build/ship_secrets.json

# local state
swgtracker_local.db*
swg_tracker_desktop.log

# python
venv/
__pycache__/
*.pyc

# build artifacts (build/ scripts stay tracked)
build/dist_bundle/
build/SWG Tracker Desktop/
build/icon.iconset/
*.spec
SWG Tracker Desktop.app/
dist/

# editors / os
.idea/
.DS_Store

